<h1> Create a Service Option for your Shipment! </h1>

<br><br>

<%= form_for(@service) do |t|%>
    <div>
        <%= t.label :name%>
        <%= t.text_field :name%>
    </div>
    
    <br>
    <p> Select a Carrier </p>
    <div>
       <%= t.collection_select :carrier_id, Service.all, :id, :name%>
    </div>

    <p> Or Create a New Carrier </p>
    <div> # This is where we will make our nested form.
        <%= t.fields_for :carrier do |c| %>
            <%= c.label :name %>
            <%= c.text_field :name %>
        <% end %>
    </div>

    <br><br>

    <%= t.submit "Generate Service"%>
<% end %>
